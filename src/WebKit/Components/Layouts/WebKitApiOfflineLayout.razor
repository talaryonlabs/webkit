@inherits LayoutComponentBase

<div class="flex flex-col min-h-screen transition-all text-base">
    <!-- Body -->
    <main class="flex-1 flex bg-secondary items-center justify-center justify-items-center">
        @Body
    </main>
</div>